# 세뱃돈 얼마나 줄까? (Apps in Toss 미니앱)

## 1. 앱 개요
- **앱 이름**: 세뱃돈 얼마나 줄까?
- **설명**: 설날을 맞아 가족, 지인에게 줄 세뱃돈 액수를 고민하는 유저들을 위한 통계 가이드 앱입니다. 유저들의 선택 데이터를 실시간으로 수집하여 연령별, 관계별 평균 및 가장 선호하는 금액을 보여줍니다.
- **주요 타겟**: 20대~50대 토스 유저 (자녀, 조카에게 세뱃돈을 주려는 사람)

## 2. 앱 로고 및 디자인
- **로고**: 600x600px, 각진 정사각형 (디자인 시안: 빨간색 복주머니와 황금색 동전 심볼)
- **UI 시스템**: 토스 디자인 시스템(TDS) 스타일을 차용하여 깨끗하고 직관적인 화이트/블루 테마 적용.

## 3. 앱인토스 출시 가이드 (Ait Release Guide)

토스 '앱인토스'에 앱을 출시하기 위한 절차입니다.

### 사전 준비
1. **토스 비즈니스 계정**: [토스 개발자 센터](https://developers-apps-in-toss.toss.im/)에서 파트너 가입이 필요합니다.
2. **미니앱 생성**: 콘솔에서 '미니앱 만들기'를 통해 고유 ID를 발급받습니다.

### 빌드 및 배포 방법
본 프로젝트는 Vite/React 기반으로 제작되었습니다.
1. **환경 변수 설정**: `.env` 파일에 Supabase URL 및 Anon Key를 등록합니다.
2. **빌드**:
   ```bash
   npm run build
   ```
3. **배포**: 앱인토스 CLI(`ait`)를 사용하여 배포를 진행할 수 있습니다.
   ```bash
   npx ait deploy
   ```
   *참고: `package.json`에 `ait` 관련 스크립트가 정의되어 있어야 합니다.*

### 검수 체크리스트 (중요)
- [ ] **다크 모드 지원**: 토스 앱 설정에 따라 UI가 깨지지 않는지 확인.
- [ ] **로딩/네트워크 처리**: 데이터 로딩 시 스켈레톤 UI 또는 인디케이터 제공.
- [ ] **디자인 가이드 준수**: 로고 크기(600x600px) 및 브랜딩 컬러 사용 규정 준수.
- [ ] **용량 제한**: 전체 번들 용량이 100MB(압축 해제 기준) 이하인지 확인.
- [ ] **Toss Bridge**: 토스 앱 내 인터랙션(토스트, 얼럿 등)을 위해 `@apps-in-toss/framework`의 브릿지 API 사용 권장.

## 4. 데이터베이스 구조 (Supabase)
이 프로토타입은 아래와 같은 `sebaetdon_surveys` 테이블을 사용합니다.
- `id`: uuid (Primary Key)
- `created_at`: timestamp
- `relationship`: text (손주, 조카 등)
- `status`: text (학생, 직장인 등)
- `age_group`: text (10대, 20대 등)
- `amount`: int8 (금액)
- `is_planned`: boolean (계획 여부)
